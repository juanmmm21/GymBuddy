/**
 * Versión que expone /health. El Worker se empaqueta sin manifiesto accesible en
 * ejecución, así que se mantiene alineada a mano con la de apps/api/package.json.
 */
export const API_VERSION = '0.1.0';
