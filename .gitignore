# ---------- Node / TypeScript ----------
node_modules/
dist/
build/
.vite/
coverage/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# ---------- Cloudflare ----------
# Estado local de wrangler: incluye la D1 de desarrollo con datos de
# entrenamiento reales. Nunca se versiona.
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# ---------- Secretos ----------
.env
.env.*
!.env.example

# ---------- Editor / SO ----------
.vscode/
.idea/
.DS_Store

# ---------- Contexto para agentes de IA (nunca en el repo público) ----------
# GymBuddy es un repo público y único: el andamiaje de trabajo con agentes y el
# roadmap se quedan en local. Un repo público no debe leerse como obra a medias.
AGENTS.md
CLAUDE.md
PROGRESS.md
START_HERE.md
.claude/
