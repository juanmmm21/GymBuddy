# El Worker y la PWA importan @gymbuddy/shared por su ruta de origen: sin hoisting
# implícito, cada paquete declara lo que usa y los fallos salen en local, no en CI.
strict-peer-dependencies=false
auto-install-peers=true
